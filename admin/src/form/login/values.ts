export const loginIniValues = {
  email:
    import.meta.env.VITE_APP_MODE === "local" ? "ratul@mgmfinanciers.com" : "",
  password: import.meta.env.VITE_APP_MODE === "local" ? "Ratul!@#123" : "",
};
